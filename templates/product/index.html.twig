{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{# {% block body %}
    <h1>Nos produits</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Type</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.type }}</td>
                <td><img src="{{ asset('uploads/images/' ~ product.image) }}" alt=""></td>
                <td>
                    <a href="{{ path('app_product_show', {'id': product.id}) }}">show</a>
                    <a href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_product_new') }}">Create new</a>
{% endblock %} #}

{% block body %}
<div class="bg-gray-200 flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 text-white p-8 rounded-lg shadow-lg w-4/5 max-w-5xl">
        <h1 class="text-center text-2xl font-bold mb-8">Nos produits</h1>
        <div class="grid grid-cols-2 gap-6">
            {% for product in products %}
                <div class="bg-white rounded-lg overflow-hidden shadow-md">
                    <img src="{{ asset('uploads/images/' ~ product.image) }}" alt="{{ product.name }}" class="w-full h-40 object-cover">
                    <div class="p-4 text-center text-gray-800 font-semibold">{{ product.name }}</div>
                    <div class="p-4 text-gray-600">{{ product.description }}</div>
                    <div class="p-4 text-gray-600">Type : {{ product.type }}</div>
                    <div class="p-4 text-center">
                        <a href="{{ path('app_product_show', {'id': product.id}) }}" class="text-blue-600 hover:underline mr-4">Afficher</a>
                        <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="text-green-600 hover:underline">Modifier</a>
                    </div>
                </div>
            {% else %}
                <div class="col-span-2 text-center text-gray-200">Aucun produit trouvé.</div>
            {% endfor %}
        </div>
        <div class="mt-8 text-center">
            <a href="{{ path('app_product_new') }}" class="bg-blue-600 text-white px-4 py-2 rounded shadow hover:bg-blue-500">Créer un nouveau produit</a>
        </div>
    </div>
</div>
{% endblock %}
