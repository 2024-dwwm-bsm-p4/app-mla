{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.category) }}
    {{ form_row(form.image) }}
    {{ form_row(form.isBio) }}
    <button type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
